import React from 'react';
import {SafeAreaView} from '../../../common/simpleComponents/SafeAreaView';
import {StyledText} from '../../../common/simpleComponents/Text';

export const GPSScreen = () => {
  return (
    <SafeAreaView>
      <StyledText>GPS</StyledText>
    </SafeAreaView>
  );
};
